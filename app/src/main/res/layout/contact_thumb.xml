<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/contact_grid_layout"
    android:orientation="vertical" android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:onClick="clickedContact">
        <ImageView
            android:id="@+id/contact_thumb"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:background="@color/background_color"
            android:scaleType="fitXY">
        </ImageView>

        <ImageView
            android:id="@+id/name_bar"
            android:layout_width="200dp"
            android:layout_height="30dp"
            android:background="#90000000"
            android:layout_alignParentTop="true"
            android:layout_marginTop="170dp"
            />

    <ImageView
        android:id="@+id/fav_star"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/star_icon"
        android:visibility="invisible"
        android:layout_alignBottom="@+id/name_bar"
        android:layout_alignEnd="@+id/contact_thumb" />

        <TextView
            android:id="@+id/contact_name"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:layout_alignParentTop="true"
            android:layout_marginTop="170dp"
            android:layout_marginLeft="5dp"
            android:textSize="20dp"
            android:textColor="#FFFFFF"
            />

</RelativeLayout>